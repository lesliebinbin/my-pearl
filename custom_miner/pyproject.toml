[project]
name = "custom-miner"
version = "0.1.0"
description = "Experimental custom miner utilities for Pearl Gateway"
requires-python = ">=3.12"

dependencies = [
    "miner-base",
    "pearl-gateway",
    "pearl-gemm",
    "py-pearl-mining",
    "torch==2.11.0",
]

[tool.uv]
package = false
